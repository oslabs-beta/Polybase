/// <reference types="react" />
interface WindowSize {
    windowWidth: number;
    windowHeight: number;
    scrollbarWidth: number;
    isReady: boolean;
}
export declare const useWindowSize: () => {
    windowSize: WindowSize;
    windowSizeRef: import("react").MutableRefObject<WindowSize>;
};
export {};
